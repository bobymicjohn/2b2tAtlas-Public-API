{
  "name": "2b2tatlas-e2e",
  "private": true,
  "description": "Playwright end-to-end harness for WDL ingestion (login -> upload -> verify).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
